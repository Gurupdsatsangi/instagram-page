<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>instagram</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

   <style>
    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    background-color:#111;
  }
    header {
      
    padding: 16px;
    background-color: #111;
    z-index: 3;
    border-bottom: 1px solid #333;
    
  }
 .nav-detail{
    display: flex;
    justify-content: space-between;
    padding: 16px;
    max-width: 935px;
    margin: 0 auto;
 }
  .logo {
    font-size: 24px;
    font-weight: bold;
    color: white;
  }
  
  .nav-buttons button {
    margin-left: 10px;
    padding: 8px 16px;
    border: none;
    background-color: #3897f0;
    color: white;
    border-radius: 4px;
    cursor: pointer;
    font-weight: bold;
  }
  
  .nav-buttons button:hover {
    background-color: #2c82d3;
  }
  .profile-header {
      display: flex;
      align-items: center;
      flex-wrap: wrap;
    }
    .main{
        padding-left: 150px ;
        padding-right: 100px;
        padding-top: 75px;
        height: 350px;
        color: #fff;
    }

    .profile-pic {
      width: 120px;
      border-radius: 50%;
      margin-right: 20px;
      background-color: #fff;
     
    }

    .profile-info {
        padding-left: 50px ;
      flex: 1;
    }

    .profile-top {
      display: flex;
      align-items: center;
      gap: 10px;
      flex-wrap: wrap;
    }

    .profile-top h1 {
      margin: 0;
      font-size: 24px;
    }

    .verified {
      color: #3897f0;
      font-size: 18px;
    }

    .profile-top button {
      padding: 6px 14px;
      background-color: #262626;
      color: white;
      border: 1px solid #333;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
    }

    .profile-top button:hover {
      background-color: #333;
    }

    .stats {
      margin: 10px 0;
    }

    .stats span {
      margin-right: 20px;
    }

    .bio {
      font-size: 14px;
    }

    .bio a {
      color: #3897f0;
      text-decoration: none;
    }
    .gallery {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;

    }
    .tabs {
      display: flex;
      gap: 20px;
      margin:  0;
      border-top: 1px solid #333;
      padding-top: 10px;
      justify-content:center ;
    }
    
  
    .post img {
      width: 100%;
      height: 600px;
      display: block;
      border-radius: 10px;
    }
    
   </style>
</head>
<body>
    <header>
        <div class="nav-detail">
        <div class="logo">Instagram</div>
        <div class="nav-buttons">
          <button>Log In</button>
          <button>Sign Up</button>
        </div>
    </div>
      </header>
      <div class="main">
      <div class="profile-header">
        <img src="<%=data.logo%>" class="profile-pic" alt="Facebook Profile Pic" />
        <div class="profile-info">
          <div class="profile-top">
            <h1><%= data.name %></h1>
            <span class="verified">✔</span>
            <button>Follow</button>
            <button>Message</button>
          </div>
          <div class="stats">
            <span><strong><%data.tpost%></strong> posts</span>
            <span><strong><%=data.followers%></strong> followers</span>
            <span><strong><%=data.following%></strong> following</span>
          </div>
          <div class="bio">
            <strong><%= data.name %></strong><br />
           <%=data.p%><br />
            <a href="<%=data.contact%>">fb.me/HowToContactFB</a>
          </div>
          
          
          </div>
        </div>
        </div>

        <section class="tabs">
         
          <div class="container text-center my-4">
            <div class="btn-group" role="group" aria-label="Basic radio toggle button group">
              
              <input type="radio" class="btn-check" name="btnradio" id="btnradio1" autocomplete="off" checked>
              <label class="btn btn-secondary me-3" for="btnradio1">Posts</label>
              
              <input type="radio" class="btn-check" name="btnradio" id="btnradio2" autocomplete="off">
              <label class="btn btn-secondary me-3" for="btnradio2">reels</label>
              
              <input type="radio" class="btn-check" name="btnradio" id="btnradio3" autocomplete="off">
              <label class="btn btn-secondary" for="btnradio3">Tagged</label>
              
            </div>
          </div>
          
        
        </section>

        <div class="gallery">
          <% for(let post of data.posts){%>
          <div class="post"><img src="<%=post.image%>" alt="Post 1" /></div>
          <%}%>
        
      
    </div>
</body>
</html>